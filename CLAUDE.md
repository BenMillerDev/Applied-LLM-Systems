# CLAUDE.md

## Project
Course repository for COSC 650: Applied LLM Systems (Maryville University).
8-week graduate course covering tokenization, transformer architecture,
prompt engineering, function calling, retrieval-augmented generation,
fine-tuning, and evaluation.

## Structure
- week-0/ : environment setup and prerequisites
- week-1/ through week-8/ : weekly assignments and notebooks
- notes/ : research notes and reading annotations (added as needed)
- project/ : final project code and documentation (added later in the course)
- CLAUDE.md : this file
- README.md : human-facing project description

## Conventions
- Notebooks are saved from Google Colab via File > Save a copy in GitHub
- All code is Python 3.11+
- tiktoken is used for tokenization experiments; the Gemini API is used for assignments that call a live model
- API keys are stored in Colab Secrets (or a local .env file when running Jupyter) — never hardcoded in a notebook
- Weekly work happens on a feature branch (e.g. week-1-tokenization), merged into main via pull request — not committed directly to main
- Commits use descriptive messages, not "update" or "fix"

## Do Not
- Delete files or directories without confirming first
- Push to main without checking what is staged
- Commit API keys or any file in .env

## AI Assistance

This section discloses how AI tools were used on specific assignments,
per the course's AI-use policy.

### Week 1 — Tokenization Analysis
Claude (Anthropic) assisted with:
- Modifying the starter notebook's Python code to work with the Vietnamese
  passage and compare word pairs
- Drafting and revising this file and the README
- Helping select the three tokenizer-bias examples from a batch-tested
  candidate list (to avoid cherry-picking), and explaining the byte-level
  cause behind the NFC/NFD failure case
- Drafting the PR description and research-note issue description (#2)

All token counts, ratios, and character counts reported in the notebook
were generated by running the tokenizer directly, not pasted from Claude
without independent verification.

### Week 2 — Inference and Sampling
Claude (Anthropic) assisted with:
- Modifying the starter notebook's Python code to build the sampling
  explorer (temperature, top-k, and top-p) and evaluate the three
  required settings against distilgpt2's real logits
- Drafting and revising this file and the README
- Drafting the PR description for Week 2
- Drafting the research-note issue body and comment for Week 2 (#4)
- Helping format the notebook's "plain language explanations" output,
  translating the raw temperature/top-k/top-p numbers into readable
  print statements, and explaining the math behind using
  `np.exp(entropy(dist))` to estimate how many tokens are realistically
  competing for the next-token pick

All token probabilities, entropy values, and effective-choice figures
reported in the notebook were generated by running the model directly.
The analysis cells, the predictions in Part 3, the comparison against actual results,
and the failure-case analysis in Part 4 reflect my own reasoning, not unverified AI output.

### Week 3 — Prompts as Engineering Artifacts
Claude (Anthropic) assisted with:
- Modifying the starter notebook's Python code to build the code review triage prompt evaluation,
  including the fixture-based test harness, and the pandas comparison table used to visualize the tradeoff
- Helping harden test case #9 so the baseline prompt actually failed on it against a live model,
  after an initial version scored perfectly on both prompt versions and would have hidden the intended tradeoff
- Drafting and revising this file and the README
- Drafting the PR description for Week 3
- Drafting the research-note issue body and comments for Week 3 (#6)

All classification results, exact-match scores, and semantic-similarity scores reported 
in the notebook were generated by running the prompts against the live model directly, 
not pasted from Claude without independent verification. The cause analysis and mitigation 
in Part 4 reflect my own reasoning, not unverified AI output.

### Week 4 — Multi-Tool Assistant
Claude (Anthropic) assisted with:
- Modifying the starter notebook's Python code to build the PR-triage
  tool set (`get_file_stats`, `classify_pr_size`, a guarded `run_python`
  code-runner restricted to arithmetic via AST whitelisting) and wire a
  real Gemini function-calling loop against them
- Drafting and revising this file and the README
- Drafting the PR description for Week 4
- Drafting the research-note issue body and comments for Week 4 (#8)

All tool-call results, error messages, and turn-by-turn call sequences
reported in the notebook were generated by running the live Gemini
function-calling loop directly, not pasted from Claude without
independent verification. The diagnosis of why the `get_file_stats`
path-guessing failure occurred, the decision to fix it with a
description change rather than a schema change, and the confirmation
that the fix resolved the failure are my own conclusions, drawn from
those runs.

### Week 5 — RAG Pipeline with Retrieval Evaluation
Claude (Anthropic) assisted with:
- Modifying the notebook's Python code to build the chunking functions
  (fixed-size and by-paragraph), the FAISS retrieval and precision/recall@3
  evaluation functions, and a diagnostic addition that prints the actual
  retrieved chunks (flagged relevant/wrong) for each disagreeing query
  instead of just the aggregate scores
- Drafting and revising this file and the README
- Drafting the research-note issue and PR body for Week 5

All precision/recall scores, retrieved-chunk traces, and generation outputs
reported in the notebook came from running the actual embedding, FAISS
retrieval, and evaluation code myself. The diagnosis of why small chunking underperforms
against larger and by-paragraph chunking, the specific retrieval-failure
trace on the analytics-dashboard query in Part 4, and the conclusions about
the precision/recall tradeoffs across chunk sizes are my own, drawn from
those runs.
